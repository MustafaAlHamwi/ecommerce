# Since .env is gitignored, you can use .env.example to build a new `.env` file when you clone the repo.
# Keep this file up-to-date when you add new variables to \`.env\`.

# This file will be committed to version control, so make sure not to have any secrets in it.
# If you are cloning this repo, create a copy of this file named `.env` and populate it with your secrets.

# The database URL is used to connect to the database
# DATABASE_URL='mysql://user:password@host/db?sslaccept=strict'
NEXT_TELEMETRY_DISABLED=1

#Local DB
DATABASE_URL="postgresql://YOUR_POSTGRES_USERNAME:YOUR_POSTGRES_PASSWORD@localhost:5432/ecommerce"

# @see https://next-auth.js.org/configuration/options#nextauth_url
NEXTAUTH_URL="http://localhost:3000"

# You can generate the secret via 'openssl rand -base64 32' on Unix
# @see https://next-auth.js.org/configuration/options#secret
NEXTAUTH_SECRET=""

# Next Auth Google Provider
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# NodeMailer auth
SMTP_HOST=mail.privateemail.com
SMTP_PORT=465
SMTP_USERNAME=noreply@ecommerce.com
SMTP_PASSWORD=test
EMAIL_FROM_NAME=ecommerce <noreply@ecommerce.com>

VERCEL_URL="https://ecommerce-testing.vercel.app"